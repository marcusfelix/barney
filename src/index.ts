// main entry point
// bootsrap order:
// - setup tools: opencode (download, install)
// - return opencode by platform
// - check for minimun requirements (log if not met)
// - create .barney folder if not exists. this folder should contain
//   - opencode.json
// - if .barney exists. use ir
// = add .barney to .gitignore if not exists
// create .context/tasks folder if not exists
// - start TUI 
